# Post — HyperFrames lane (rich)

Agent-authored motion graphics and overlays on top of take media.

```bash
reactable takes hf init <id>
reactable takes hf render <id>
```

Also load skills: **hyperframes**, **hyperframes-cli**, **gsap** (for motion).

See [takes-hyperframes.md](../references/takes-hyperframes.md).

Install HyperFrames skills: `npx hyperframes init`
