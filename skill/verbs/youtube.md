# YouTube — search and embed

```bash
reactable youtube connect
reactable youtube search "query" --json
reactable youtube proxy --video <id> [--start 0] --json
```

Deck slides: `type: youtube`, `videoId: …` in `deck.work`.

OAuth client: `~/.shinyobjectz/youtube_client.json` (same as studio).
