# Native
native/.build/
native/spike-out/
dist/

# Rust tools
tools/target/

# Sidecar (Elixir)
sidecar/_build/
sidecar/deps/
sidecar/burrito_out/
*.dump

# Web
web/node_modules/
web/.wrangler/

# Runtime / local
.reactable/
.nexus/
.wb-data/
takes/
edit-intel/

# build artifacts, large model binaries, vendored tools, downloaded media — not source
node_modules/
.build/
*.mlpackage
vendor/ffmpeg
assets/
ios/build/
*.mov
.env

# Eval run artifact (report.work is the committed baseline; raw json churns)
scripts/eval/last-run.json

# OS
.DS_Store

# Bun
node_modules/

# eval run artifacts (harness is committed; outputs are not)
scripts/eval/video-debug.json
scripts/eval/video-last-run.json
scripts/eval/video-run-2trial.txt
